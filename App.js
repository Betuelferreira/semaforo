
import { Button, StyleSheet, Text, View } from 'react-native';
import Quad from './componentes/quad';
export default function App() {

  

  return (
    <View style={{flex:1, alignItems: 'center', justifyContent: 'center' }}>
     <Quad></Quad>
    </View>
  );

};